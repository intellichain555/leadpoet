# Provider cost configuration for Lead Sorcerer
# All costs are in USD per unit

openrouter:
  unit: '1k_tokens'
  usd_per_unit: 0.0020

serper:
  unit: 'request'
  usd_per_unit: 0.0005

crawl4ai:
  unit: 'page_fetch'
  usd_per_unit: 0.0000

llm_extract:
  unit: 'extraction'
  usd_per_unit: 0.0010
